VITE_API_BASE_URL=https://immo-find-api.onrender.com

# For local development you can comment out the line above to use the Vite proxy
# (configured in vite.config.ts) which forwards /api/* to http://localhost:8000
